
class Pila:
    def contructor(self,tamaño):
        lista = [self.tamaño]
        espejo = [self.tamaño]
    def pop():        
        lista [lista.length] = None
        return True
    def peek():
        for i in range(lista.length):
            if(lista[i] != None):

            
        else:
            return False
    def push(self, object):
        
